# Day 8 – Gradient Descent Intuition

Gradient Descent is an optimization algorithm used to minimize loss.

Idea:
- Start with random parameters
- Measure error
- Move parameters in the direction that reduces error
- Repeat until loss is minimum

Gradient Descent helps the model learn the best values for parameters.
